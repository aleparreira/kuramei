# Terraform AWS Provider Configuration
# Generated by Kuramei

terraform {
  required_version = ">= 1.0.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "{{ region }}"

  default_tags {
    tags = {
      ManagedBy   = "terraform"
      GeneratedBy = "kuramei"
    }
  }
}
